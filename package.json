{
  "name": "whisperx-diarized-archive",
  "private": true,
  "packageManager": "pnpm@10.23.0",
  "scripts": {
    "doctor": "node scripts/doctor.mjs",
    "predev": "node scripts/doctor.mjs",
    "dev": "sh scripts/dev.sh",
    "dev:web": "pnpm --filter @whisperx-diarized-archive/web dev",
    "dev:api": "cd services/api && .venv/bin/uvicorn main:app --reload --port ${API_PORT:-8000}",
    "build": "pnpm --filter @whisperx-diarized-archive/web build",
    "typecheck": "pnpm --filter @whisperx-diarized-archive/web typecheck",
    "lint": "pnpm --filter @whisperx-diarized-archive/web lint",
    "lint:api": "cd services/api && .venv/bin/ruff check .",
    "test:api": "cd services/api && .venv/bin/python -m pytest",
    "check:structure": "cd services/api && .venv/bin/python -m pytest tests/test_structure.py -v",
    "test:e2e": "pnpm --filter @whisperx-diarized-archive/web exec playwright test",
    "batch:transcribe": "cd services/api && .venv/bin/python scripts/batch_transcribe.py"
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  },
  "engines": {
    "node": ">=20",
    "pnpm": ">=9"
  }
}
